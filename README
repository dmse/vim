# installation:
git clone https://github.com/dmse/vim.git

## create symlinks
ln -s ~/.vim/vimrc ~/.vimrc

## remove symlinks
# unlink ~/.vimrc

## fetch submodules
# cd ~/.vim/bundle
git submodule init
git submodule update
